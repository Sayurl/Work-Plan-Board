// Reserved for v0.3.x timeline work. TimeBlock represents a fixed time component
// such as a meeting, site visit, travel, or planned work block.
const TIME_BLOCK_RELATION_TYPES = ["inside", "before", "after", "related"];

module.exports = {
  TIME_BLOCK_RELATION_TYPES
};
