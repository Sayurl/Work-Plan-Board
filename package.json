{
  "name": "work-plan-board",
  "version": "0.3.1",
  "private": true,
  "description": "Plan tasks and scheduled work across projects in one Obsidian board.",
  "scripts": {
    "build": "node scripts/build.js",
    "check": "node scripts/check.js",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
